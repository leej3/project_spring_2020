version: 2
jobs:
    build:

        docker:
          - image: continuumio/anaconda3
        working_directory: /tmp/src
        steps:
          - checkout
          - run:
              command: |
                cd project_spring_2020/BIOF309_RDK
                pip install .
                pytest  --showlocals tests
